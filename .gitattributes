# 统一按 LF 存储，避免在 Windows 上出现整文件行尾差异
* text=auto eol=lf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary

# 锁文件在 diff 里折叠显示
package-lock.json -diff linguist-generated
