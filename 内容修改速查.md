# 改哪里：内容速查表

想改某个东西时，按这张表找文件。**行号会随编辑变动，以字段名为准。**

---

## 一、站点全局（`src/site.config.ts`）

这个文件是配置总入口，大部分"站级"设置都在这里。

| 想改什么 | 字段 | 大约行号 |
| --- | --- | --- |
| 站点标题（浏览器标签、页脚版权） | `title` | 6 |
| 作者名（首页大标题、页脚） | `author` | 8 |
| 站点简介（SEO 描述、友链互通信息） | `description` | 10 |
| 站点图标路径 | `favicon` | 12 |
| 社交分享卡片图片 | `socialCard` | 14 |
| 语言与日期格式 | `locale` | 16-26 |
| 首页头像 | `logo.src` | 29 |
| 顶部导航菜单 | `header.menu` | 43-48 |
| 页脚年份 | `footer.year` | 54 |
| 页脚额外链接（备案号等） | `footer.links` | 55 |
| 页脚社交图标 | `footer.social` | 59-63 |
| 博客每页文章数 | `content.blogPageSize` | 75 |
| 文章分享按钮 | `content.share` | 78 |
| 友链互通信息（友链页展示） | `integ.links.applyTip` | 95-101 |
| 友链变更记录 | `integ.links.logbook` | 89 |
| 首页底部一言的接口 | `integ.quote` | 105-113 |
| 正文排版（字体大小、行高） | `integ.typography` | 118-125 |
| 图片点击放大 | `integ.mediumZoom` | 132-140 |
| 评论系统（已关闭） | `integ.waline` | 145-150 |

---

## 二、各个页面

| 想改什么 | 文件 |
| --- | --- |
| **首页**：个人介绍文字、位置/GitHub 标签、教育经历、技能标签 | `src/pages/index.astro` |
| **关于页**：自我介绍、建站说明、联系方式、时间线 | `src/pages/about/index.astro` |
| **项目页**：项目、命题经历、题解列表 | `src/pages/projects/index.astro` |
| **友链页**：页面说明文字、申请规则 | `src/pages/links/index.astro` |
| **404 页** | `src/pages/404.astro` |

### 首页细节

| 想改什么 | 位置 |
| --- | --- |
| 技能分组的内容 | `src/pages/index.astro` 顶部第 13-16 行的四个数组 |
| 位置标签（现在是 "China"） | `src/pages/index.astro` 的 `<Label title='China'>` |
| GitHub 链接 | `src/pages/index.astro` 的 `<Label title='GitHub' ...>` |
| 教育经历（学校名、学位、时间、校徽） | `src/pages/index.astro` 的 `<Section title='教育经历'>` 内两个 `<Card>` |
| 分区标题（关于/文章/教育经历/技能） | `src/pages/index.astro` 的 `<Section title='...'>` |

### 关于页细节

| 想改什么 | 位置 |
| --- | --- |
| 身份那一行（现在是"开发者 / 终身学习者"） | `<h2 id='about-me'>` 下面第一个 `<p>` |
| 自我介绍段落 | `<h2 id='about-me'>` 区块 |
| 建站说明与技术栈 | `<h2 id='about-blog'>` 区块 |
| 邮箱 / GitHub | `<h2 id='contact'>` 区块 |
| 时间线事件 | `<Timeline events={[...]}>` |

### 项目页细节

| 想改什么 | 位置 |
| --- | --- |
| 分区标题 | 文件顶部 `const headings` |
| 项目列表 | `const projects = [...]` |
| 命题与竞赛 | `const contests = [...]` |
| 笔记与题解 | `const learnings = [...]` |

每个条目的写法：

```js
{
  name: '项目名',
  description: '一句话说明',
  links: [
    { type: 'github', href: 'https://github.com/...' },  // 图标可选：github / site / doc / release
    { type: 'site', href: 'https://...' }
  ]
}
```

> 没有链接的条目（比如纯经历）可以**省略 `links`**，标题会自动降级成纯文本。

---

## 三、文章

| 想改什么 | 位置 |
| --- | --- |
| 新增/编辑文章 | `src/content/blog/` 下新建 `.md` 文件 |
| 文章必填字段的定义 | `src/content.config.ts`（第 20-40 行） |

文章 frontmatter **必填三项**：`title`、`publishDate`、`description`。

```markdown
---
title: 文章标题              # 最长 60 字
publishDate: 2026-02-05
description: 一句话摘要       # 最长 160 字
tags: [算法, 题解]           # 可选，会自动转小写
language: 中文               # 可选
draft: false                 # 可选，true 则不发布
---
```

---

## 四、友链列表（`public/links.json`）

三个分组，按 `id_name` 区分：

| 分组 | 用途 |
| --- | --- |
| `cf-links` | 常用友链 |
| `inactive-links` | 不太活跃的好友 |
| `special-links` | 其他站点（已有虫洞、开往） |

单个友链的写法：

```json
{
  "name": "站点名",
  "intro": "一句话介绍",
  "link": "https://example.com",
  "avatar": "https://example.com/avatar.png"
}
```

---

## 五、图片与图标

| 想改什么 | 怎么改 |
| --- | --- |
| **头像**（首页、关于页共用） | 替换 `src/assets/avatar.jpg` |
| **站点图标** | 跑 `node scripts/gen-luotianyi-icon.mjs`（改脚本里的配色） |
| **社交分享卡片** | 替换 `public/images/social-card.png`，或改 `scripts/gen-assets.mjs` 重跑 |
| **学校校徽** | 替换 `public/images/shandong-university.png`、`jinan-zhensheng.png` |
| 关于页右下角签名 | `src/assets/signature.svg` |

> 注意：头像是 `src/assets/avatar.jpg`，**不在 `public/` 里**。它经过 Astro 的图片管线处理，
> 所以没有固定 URL。如果别处需要引用头像的公开地址，用 `public/` 下真实存在的文件。

---

## 六、样式与配色

| 想改什么 | 文件 |
| --- | --- |
| **主题色、背景、边框、圆角** | `src/assets/styles/app.css`（浅色在前，深色在 `.dark` 块） |
| 文章正文排版 | `src/assets/styles/global.css` |
| 代码块样式 | `astro.config.ts` 的 `shikiConfig` |
| 友链页好友圈样式 | `src/assets/styles/fc.css`（目前未启用） |

`app.css` 里的变量用 HSL 三段式写法，例如主题色 `--primary: 200 29% 45%` 表示
`hsl(200, 29%, 45%)`。改色号时三个数都要改。

---

## 七、部署与站点地址

| 想改什么 | 文件 |
| --- | --- |
| **站点地址**（影响 SEO、RSS、sitemap） | `astro.config.ts` 第 34 行 `site:` |
| URL 是否带尾斜杠 | `astro.config.ts` 第 38 行 `trailingSlash` |
| 自动部署流程 | `.github/workflows/deploy.yml` |
| 依赖与脚本命令 | `package.json` |

> 换自定义域名时：改 `astro.config.ts` 的 `site`，并在 `public/` 下加一个
> `CNAME` 文件（内容为域名），然后在 GitHub 仓库 Settings → Pages 里配置。

---

## 八、常用命令

```bash
npm run dev        # 本地开发，改代码即时刷新
npm run build      # 构建到 dist/（含搜索索引）
npm run preview    # 预览构建结果
npm run check      # 类型检查
npm run clean      # 清理 .astro 与 dist
```

推送即部署，等 1～2 分钟生效。
